const Projects = (props) => {

    return (
        <div>
            <img src={props.img} className="hover transition-opacity bg-blue-400 hover:opacity-30"/>
        </div>

    )

}

export default Projects